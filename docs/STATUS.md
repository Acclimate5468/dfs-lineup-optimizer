# Portfolio Snapshot Status

This repository is a sanitized source snapshot with fresh Git history.

Included:

- Streamlit application and Classic workflow pages
- Core ingestion, matching, persistence, projection, alert, optimizer, and export modules
- Separate experimental Captain modules
- Synthetic fixtures, directory placeholders, technical design documents, and tests

Excluded:

- Prior Git history and commit metadata
- Local databases, uploads, exports, logs, state, and environment files
- Internal AI workflow prompts and checkpoint notes
- Personal identifiers and machine-specific paths

Validation note: the automated tests are retained but were not executed as part of the connector-only migration. Current official salary/odds feed compatibility should be verified separately.
